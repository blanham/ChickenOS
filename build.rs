use std::env;
use std::process::Command;
use std::path::Path;

fn main() {
    //let out_dir = env::varo("OUT_DIR").unwrap();

    //let src_dir = Path::new("src");

    //assert!(env::set_current_dir(&src_dir).is_ok());

    //let make_output = Command::new("make").arg("-f").arg("Makefile.rust").output().unwrap();

    //assert!(make_output.status.success())



    //println!("Out dir: {}", out_dir);
}
