;switch(thread_t *cur, thread_t *new)

;need to get offsets to place to store stack pointer

;push regs

;save stack pointer to thread struct


;move stack pointer from new thread to esp

;pop regs

;ret, which goes back to wherever switch was called from
