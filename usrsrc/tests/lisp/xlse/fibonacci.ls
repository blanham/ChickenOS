/************************************************
*
* LISP Test Program 
*
************************************************/

// (define fib (lambda (x) (cond ((= x 0) 0) ((= x 1) 1) (#T (+ (fib (- x 1)) (fib (- x 2)))))))
 
(define fib (lambda (x)
  (cond
    ((= x 0) 0)
    ((= x 1) 1)
    (#T (+ (fib (- x 1)) (fib (- x 2))))
  )
))

(fib 5)